<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Rankings</title>
  <link rel="shortcut icon" href="/images/award.svg" type="image/x-icon">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
    crossorigin="anonymous"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    .table th, .table td {
      vertical-align: middle;
    }
    .table-hover tbody tr:hover {
      background-color: #f8f9fa;
      cursor: pointer;
    }
    .fa-medal {
      font-size: 1.5rem;
    }
    .table th {
      background-color: #f1f1f1;
    }
    .table td {
      font-weight: 500;
    }
    .rank-icon {
      font-size: 1.2rem;
      margin-right: 5px;
    }
   /* Định dạng các thứ hạng */
.rank-1 {
  color: #FFD700; /* Màu vàng cho hạng 1 */
  background-color: #FFF8E1; /* Màu nền vàng nhạt */
}
.rank-2 {
  color: #C0C0C0; /* Màu bạc cho hạng 2 */
  background-color: #F1F1F1; /* Màu nền xám sáng */
}
.rank-3 {
  color: #CD7F32; /* Màu đồng cho hạng 3 */
  background-color: #F4E1D2; /* Màu nền đồng nhạt */
}

  </style>
</head>
<body>
  <%- include('../../inc/top.ejs') %>  

  <div class="container my-4">
    <h1 class="mb-4 text-center">User Rankings</h1>
    <div class="table-responsive">
      <table class="table table-bordered table-striped table-hover">
        <thead>
          <tr>
            <th class="text-center" style="width: 80px;">Rank</th>
            <th>Account Name</th>
            <th class="text-center">NFT Count</th>
          </tr>
        </thead>
        <tbody>
          <% users.forEach(user => { %>
            <tr class="<%= user.rank === 1 ? 'rank-1' : user.rank === 2 ? 'rank-2' : user.rank === 3 ? 'rank-3' : '' %>">
              <td class="text-center">
                <% if (user.rank === 1) { %>
                  <i class="fas fa-medal text-warning rank-icon"></i> 
                <% } else if (user.rank === 2) { %>
                  <i class="fas fa-medal text-secondary rank-icon"></i>
                <% } else if (user.rank === 3) { %>
                  <i class="fas fa-medal text-bronze rank-icon"></i>
                <% } %>
                <span><%= user.rank %></span>
              </td>
              <td><%= user.nameAccount %></td>
              <td class="text-center"><%= user.nftCount %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>
</body>
</html>
